import SwiftUI

// Color+Theme.swift는 Colors.swift의 hex 기반 컬러를 재노출합니다.
// 새로운 컬러는 Colors.swift에 추가하세요.
extension Color {
    /// 스플래시/온보딩 배경색 (크림)
    static let splashBackground = Color(hex: "#F5F4EC")
}

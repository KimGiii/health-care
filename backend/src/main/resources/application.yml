spring:
  application:
    name: healthcare-api
  jackson:
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:healthcare_db}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          batch_size: 50
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2000ms
  flyway:
    enabled: true
    locations: classpath:db/migration
  threads:
    virtual:
      enabled: true

server:
  port: 8080
  compression:
    enabled: true
    mime-types: application/json

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when-authorized
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  metrics:
    tags:
      application: healthcare-api
    distribution:
      # HTTP 요청 지연 percentile(p95/p99) 계산용 히스토그램 버킷 노출.
      # 미설정 시 _bucket 메트릭이 없어 Grafana 지연 패널이 No data.
      percentiles-histogram:
        http.server.requests: true
  prometheus:
    metrics:
      export:
        enabled: true

app:
  jwt:
    # 기본값 없음 — 환경변수 JWT_SECRET 또는 프로파일별 yaml에서 명시 필수.
    # 미설정 시 Spring이 PlaceholderResolutionException으로 시작 단계에서 실패.
    secret: ${JWT_SECRET}
    # Access token TTL은 짧게(1h) — refresh token으로 재발급 (iOS APIClient가 자동 처리).
    access-token-expiry-hours: 1
    refresh-token-expiry-days: 30
  s3:
    bucket: ${S3_BUCKET:healthcare-photos-local}
    region: ${S3_REGION:ap-northeast-2}
    endpoint: ${S3_ENDPOINT:}
    path-style-access: ${S3_PATH_STYLE_ACCESS:false}
    upload-prefix: ${S3_UPLOAD_PREFIX:progress-photos}
    meal-upload-prefix: ${S3_MEAL_UPLOAD_PREFIX:meal-photos}
  photo:
    signed-url-ttl-minutes: 15
  ai:
    meal:
      openai-api-key: "${OPENAI_API_KEY:}"
      openai-base-url: ${OPENAI_BASE_URL:https://api.openai.com}
      model: ${OPENAI_MEAL_MODEL:gpt-4.1-mini}
  cache:
    food-search-ttl-days: 30
    user-profile-ttl-minutes: 60
  notifications:
    max-per-day: 2
  food-api:
    public-api-key: "${PUBLIC_FOOD_API_KEY:}"
    processed-food-api-url: https://api.data.go.kr/openapi/tn_pubr_public_nutri_process_info_api
    general-food-api-url: https://api.data.go.kr/openapi/tn_pubr_public_nutri_food_info_api
  # CORS 허용 origin은 환경별 application-*.yml에서 명시 (와일드카드 금지).
  # 누락 시 시작 단계에서 IllegalStateException으로 실패.
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
  rate-limit:
    # X-Forwarded-For 헤더 신뢰 여부 — 신뢰된 프록시(Nginx/LB) 뒤에 있을 때만 true.
    # 프로덕션은 application-prod.yml에서 명시. 기본 false로 IP 스푸핑 방지.
    trust-forwarded-headers: false
